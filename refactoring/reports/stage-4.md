### Результат проверки — этап 4

- [x] Автотесты написаны (секция тестировщика закрыта)
- [x] Автотесты зелёные (`pytest` без ошибок)
- [x] Код-ревью проведено, саммари заполнено
- [x] Вердикт MERGE — этап можно закрывать

**Команда:** `pytest tests/`
**Результат прогона:** 82 passed / 0 failed (с `DATABASE_URL` на `*:5433/Kate_fitness_test`); без URL — юниты сервиса зелёные, интеграция скипается как остальные DB-тесты
**Вердикт ревьювера:** MERGE
**Саммари:** `ClientService` создаёт и ищет клиента, отдаёт списки недавно ходивших и всех. Хендлер «➕ Новый клиент» вызывает `create`, а не `sql.search_client` / `sql.insert_client_data`; списки в сценариях записи — `list_recent` / `list_all`. Невалидный телефон — `InvalidPhoneError` до INSERT; повтор — `DuplicateClientError` (в том числе гонка PK). Сообщения формата и «уже существует» разделены. `client` = телефон не менялся. Блокеров нет.
**Дата:** 23.08.2026
