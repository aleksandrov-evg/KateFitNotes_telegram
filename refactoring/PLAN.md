# План доработок KateFitNotes

Рабочий план по [REVIEW.md](../REVIEW.md). Цель: вынести бизнес-логику из Telegram-хендлеров, покрыть её автотестами и подготовить общий бэкенд для будущего сайта.

**Правило среза:** этап закрыт только когда зелёные автотесты **и** ревьювер дал `MERGE`. Следующий этап не начинать без этого.

Дата плана: 22.08.2026.

---

## Статус этапов

| Этап | Тема | Автотесты | Проверка | Можно мержить |
|------|------|-----------|----------|---------------|
| 0 | Каркас тестов и миграции as-is | ☑ | ☑ | ☑ |
| 1 | Чистые функции и критичные баги | ☑ | ☑ | ☑ |
| 2 | Конфиг, Docker, логирование | ☑ | ☑ | ☑ |
| 3 | Репозиторий и соединения | ☑ | ☑ | ☑ |
| 4 | Сервис клиентов | ☐ | ☐ | ☐ |
| 5 | Персональная запись | ☐ | ☐ | ☐ |
| 6 | Групповая запись | ☐ | ☐ | ☐ |
| 7 | Предоплата | ☐ | ☐ | ☐ |
| 8 | Отчёт по прибыли | ☐ | ☐ | ☐ |
| 9 | Состояние сценария по `chat_id` | ☐ | ☐ | ☐ |
| 10 | Бот как тонкий адаптер | ☐ | ☐ | ☐ |
| 11 | Модель данных под сайт | ☐ | ☐ | ☐ |
| 12 | HTTP API и авторизация | ☐ | ☐ | ☐ |

Легенда проверки: `☐` не начато · `☑` принято · `☒` отклонено (вернуть на доработку).

---

## Роли субагентов

На **каждом** этапе запускаются три субагента. Они не подменяют друг друга.

| Роль | Когда | Что делает | Чего не делает |
|------|-------|------------|----------------|
| **Разработчик** | первым | Реализует задачи этапа, правит прод-баги из списка этапа, логирует исключения | Не пишет полный набор автотестов этапа; не коммитит секреты; не меняет смысл `client` = телефон до этапа 11 |
| **Тестировщик** | после разработчика (или параллельно по контракту этапа) | Пишет автотесты как **обязательный результат этапа**; прогоняет их; фиксирует красные/зелёные | Не «чинит прод», чтобы подогнать тест, без явного бага; не ходит в прод-БД |
| **Код-ревьювер** | после кода и тестов | Читает diff этапа, проверяет покрытие критерия готовности, пишет саммари и вердикт `MERGE` / `NO MERGE` | Не пишет фичи и тесты; не мержит сам |

Порядок внутри этапа:

```
Разработчик  →  Тестировщик  →  Код-ревьювер  →  Результат проверки
     │                │                 │
  код этапа      автотесты        саммари + MERGE/NO MERGE
```

Если ревьювер ставит `NO MERGE`, этап возвращается разработчику. Тестировщик дополняет тесты, если ревью указало дыры в покрытии. Цикл повторяется, пока не будет `MERGE`.

### Промпт: разработчик

```
Роль: разработчик. Репозиторий KateFitNotes_telegram.
Работай только по этапу N из refactoring/PLAN.md.
Сделай все чекбоксы секции «Разработчик» этого этапа.
Не пиши полный набор автотестов (это тестировщик).
Не выходи за границы этапа. Коммиты не создавай, пока не попросили.
Отвечай на русском. В конце: список изменённых файлов и что осталось тестировщику.
```

### Промпт: тестировщик

```
Роль: тестировщик автотестов. Репозиторий KateFitNotes_telegram.
Этап N из refactoring/PLAN.md уже реализован разработчиком.
Напиши автотесты из секции «Тестировщик» этого этапа.
Результат этапа — зелёный pytest по этим тестам.
Запрещено: прод-БД, живой Telegram, токен из config.ini.
Используй tests/, фикстуры, pg_db_test / миграции — как указано в этапе.
Отвечай на русском. В конце: команда запуска, сколько тестов, красные если есть.
```

### Промпт: код-ревьювер

```
Роль: код-ревьювер. Репозиторий KateFitNotes_telegram.
Проверь diff этапа N из refactoring/PLAN.md (код разработчика + автотесты).
Не меняй код. Сформируй саммари по шаблону «Код-ревью» этапа:
- что сделано относительно критерия готовности;
- риски / дыры в тестах;
- блокеры;
- вердикт: MERGE или NO MERGE (однозначно).
Этап нельзя считать закрытым без этого саммари.
Отвечай на русском.
```

---

## Общие правила (все этапы)

- Ответы и коммиты — на русском, без упоминания Cursor.
- Не коммитить `config.ini`, `.env`, `pg_db/`, `.idea/`, токены.
- Новые SQL-запросы — с `%s` / параметрами, соединения закрывать.
- Голый `except:` не добавлять; логировать `logging.exception`.
- Не менять формат `client_list` `{1,2,3}` и смысл `client` = телефон до этапа 11.
- Не внедрять FSM-фреймворк и не писать сайт, пока нет API (этап 12).
- `test.py` не образец: тесты живут в `tests/`.
- Бот однопользовательский до этапа 9; с этапа 9 состояние ключуется `chat_id`.
- Прод-данные не использовать в тестах. Отдельный `DATABASE_URL` / `pg_db_test:5433`.

---

## Шаблон закрытия этапа

Копировать в конец этапа (или в `refactoring/reports/stage-N.md`) после прогона субагентов.

```md
### Результат проверки — этап N

- [ ] Автотесты написаны (секция тестировщика закрыта)
- [ ] Автотесты зелёные (`pytest` без ошибок)
- [ ] Код-ревью проведено, саммари заполнено
- [ ] Вердикт MERGE — этап можно закрывать

**Команда:** `pytest …`
**Результат прогона:** N passed / N failed
**Вердикт ревьювера:** MERGE | NO MERGE
**Саммари:** (1–2 абзаца: что проверено, блокеры, можно ли мержить)
**Дата:**
```

---

## Этап 0. Каркас тестов и миграции as-is

**Зачем:** без воспроизводимой схемы и `tests/` остальные этапы нельзя страховать автотестами.

**Критерий готовности:** из git поднимается тестовая БД по миграции; `pytest` запускается и не ходит в прод.

### Разработчик

- [x] Каталог `tests/` (не один `test.py`)
- [x] `pytest` в dev-зависимостях; `test.py` убрать из образа / gitignore-ловушки (починить или удалить, не оставлять синтаксически битым)
- [x] Alembic (или эквивалент): снимок текущей схемы `main.*` **без смены модели**
- [x] Seed типов тренировок для тестов
- [x] Фикстура соединения: откат транзакции или truncate; отдельный `DATABASE_URL`
- [x] Запрет тестов на прод-БД (явная проверка имени БД / порта)
- [x] `.env.example` без секретов; в README/AGENTS не дублировать токены

### Тестировщик

- [x] `tests/conftest.py`: фикстуры БД, фабрики клиента и типа тренировки
- [x] Смоук: миграция применяется на пустую БД, таблицы `client` / `trains` / `schedule` / `accounting` существуют
- [x] Смоук: seed типов тренировок читается
- [x] Негатив: pytest падает или скипается, если указана прод-БД

### Код-ревью

- [x] Саммари заполнено
- [x] Вердикт: `MERGE` / `NO MERGE`

**Саммари ревью:**

```
Вердикт: MERGE
Что сделано: Alembic 0001 переписан по DDL БД Kate_fitness (backups/kate_fitness_schema.sql). Таблицы client/trains/schedule/accounting/price/test; нет payment. Seed 17 типов из COPY main.trains. Guard против Kate_fitness и порта 5432 без изменений. pytest tests/ — 12 passed.
Риски: кластерный дамп с ПДн в gitignore; тестовая фикстура каждый session дропает schema main, чтобы та же revision id накатилась заново. client_list в БД — bigint[], бот шлёт строку {1,2,3}.
Блокеры: нет
```

### Результат проверки

- [x] Автотесты написаны
- [x] Автотесты зелёные
- [x] Код-ревью проведено
- [x] Можно мержить

**Прогон:** `pytest tests/` — 12 passed (с `DATABASE_URL` на `*:5433/Kate_fitness_test`)  
**Вердикт:** MERGE  
**Саммари:** Этап 0 перезапущен по дампу Kate_fitness: схема из git совпадает с продом, pytest не ходит в Kate_fitness:5432. Можно переходить к этапу 1.

---

## Этап 1. Чистые функции и критичные баги

**Зачем:** юнит-тесты без Telegram и БД; закрыть баги, которые ломают прод-сценарии.

**Критерий готовности:** валидация и расчёты вызываются как чистые функции; баги телефона/даты/`now()`/деления на ноль закрыты и покрыты тестами.

### Разработчик

- [x] Вынести из `bot.py` чистые функции (без `message` / `bot.send_message`):
  - нормализация телефона (`+7` / `7` / `8` → 10 цифр)
  - разбор предоплаты `"<сумма> <количество>"`
  - рабочие слоты 07:00–23:00 минус занятые
  - навигация недели (prev / current / next)
  - правило «последняя предоплаченная тренировка → `is_complete`»
- [x] Баг: невалидный телефон — сообщение о формате, не «клиент уже существует» (`validate_phone` вернул `0`)
- [x] Баг: в выборе времени в текст уходит дата, не `operation`
- [x] Баг: `'now()'` в INSERT accounting — реальная `now()` / `NOW()`
- [x] Баг: деление `summ / count_train` — отказ при нуле тренировок
- [x] Убрать голый `except:` в insert клиента и предоплаты; `logging.exception`

### Тестировщик

- [x] Телефон: `+7…`, `7…`, `8…`, мусор, короткий номер, `0`
- [x] Цена: только цифры, пусто, не-число
- [x] Предоплата: `"1000 10"`, лишние пробелы, ноль тренировок, одна часть
- [x] Слоты: полный день 07–23; занятый час исчезает; границы 6:00 и 23:00
- [x] Неделя: prev / current / next относительно опорной даты
- [x] `is_complete`: при последней тренировке пакета — true, иначе false
- [x] Регрессия: невалидный телефон не классифицируется как «уже существует»

### Код-ревью

- [x] Саммари заполнено
- [x] Вердикт: `MERGE` / `NO MERGE`

**Саммари ревью:**

```
Вердикт: MERGE
Что сделано: Чистые функции в src/Kate_Fit_Notes/domain.py (телефон, цена, предоплата, слоты, неделя, is_complete, NOW()). bot.py вызывает их без send_message в домене. Невалидный телефон → формат, не «уже существует»; подпись времени с датой; INSERT accounting с NOW() без кавычек; 0 тренировок отвергается parse_prepaid / price_per_train; except Exception + logging.exception в insert_data и предоплате. Слоты — разность множеств, не XOR. tests/test_domain.py — 29 passed, без Telegram и БД.
Риски: work_hour в bot.py больше не используется (слоты из domain). set_is_complete_true по-прежнему id или False — контракт sql.insert_in_schedule, этап 7. DB-тесты этапа 0 скипаются без DATABASE_URL.
Блокеры: нет
```

### Результат проверки

- [x] Автотесты написаны
- [x] Автотесты зелёные
- [x] Код-ревью проведено
- [x] Можно мержить

**Прогон:** `pytest tests/` — 35 passed / 6 skipped (DB без DATABASE_URL; с URL — те же 6 смоуков этапа 0)  
**Вердикт:** MERGE  
**Саммари:** Домен вынесен и покрыт юнитами; критичные баги телефона, даты, NOW(), деления на ноль и голого except закрыты. Можно переходить к этапу 2.

---

## Этап 2. Конфиг, Docker, логирование

**Зачем:** одинаковый запуск локально / в контейнере / в CI; меньше флапающих стартов.

**Критерий готовности:** бот и тесты берут конфиг из env; Docker-команда запускает бота; Postgres поднимается с healthcheck.

### Разработчик

- [x] Один источник конфига: env; `config.ini` только локальный fallback
- [x] Исправить `POSTGRESS_DB` → `POSTGRES_DB` в compose (прод и test)
- [x] Dockerfile: `CMD` как `python3 bot.py` (не `python3 -m bot.py`); не копировать сломанный `test.py`
- [x] Healthcheck Postgres + `depends_on: condition: service_healthy` для бота
- [x] `logging` вместо `print` в SQL/боте; не логировать полный SQL с телефонами в проде
- [x] Runtime / dev зависимости раздельно в `pyproject.toml`; убрать неиспользуемый `keyboa` из runtime, если не нужен
- [x] Не коммитить `.idea/` (gitignore), если ещё попадает в индекс — только по согласованию, отдельным шагом

### Тестировщик

- [x] Тест загрузки конфига: из env; fallback на ini; ошибка при отсутствии обязательных полей
- [x] Тест, что тестовый runner не подхватывает прод-`DATABASE_URL` по умолчанию
- [x] Смоук-сборка образа **или** проверка Dockerfile (CMD, COPY) — минимум: unit на парсер compose/env-примера, если полный build в этапе не гоняют
- [x] Документированная команда: `pytest tests/` проходит без живого Telegram-токена

### Код-ревью

- [x] Саммари заполнено
- [x] Вердикт: `MERGE` / `NO MERGE`

**Саммари ревью:**

```
Вердикт: MERGE
Что сделано: load_settings в src/Kate_Fit_Notes/settings.py (env, fallback config.ini, ConfigError). bot.py/sql.py читают Settings, не ConfigParser напрямую. Compose: POSTGRES_DB=Kate_fitness / Kate_fitness_test, pg_isready, depends_on service_healthy, TG_TOKEN и SQL_* у бота. Dockerfile CMD python3 bot.py, без COPY config.ini/test.py. logging вместо print; execute_query пишет statusmessage, не cursor.query. .idea/ в gitignore без git rm --cached. tests/test_settings.py + test_docker_config.py; pytest tests/ без TG_TOKEN.
Риски: POSTGRES_DB только при первой инициализации тома. docker inspect при пустом host (этап 3) по-прежнему возвращает код выхода, не IP. .idea/ ещё может быть в индексе. Смоук Docker — парсинг файлов, не docker build. import bot.py/sql.py всё ещё требует полный конфиг (этап 10).
Блокеры: нет
```

### Результат проверки

- [x] Автотесты написаны
- [x] Автотесты зелёные
- [x] Код-ревью проведено
- [x] Можно мержить

**Прогон:** `pytest tests/` — 53 passed / 6 skipped (DB без DATABASE_URL)  
**Вердикт:** MERGE  
**Саммари:** Бот и тесты берут конфиг из env (ini — fallback); Docker CMD и POSTGRES_DB исправлены, Postgres с healthcheck; SQL без полного запроса с телефонами в логах. Можно переходить к этапу 3.

---

## Этап 3. Репозиторий и соединения

**Зачем:** безопасный SQL, закрытие соединений, подмена слоя данных в тестах.

**Критерий готовности:** запросы параметризованы; соединение в `with` / пул; вызывающий код не зависит от `['SELECT', n, rows]`.

### Разработчик

- [x] Интерфейс репозитория вместо «голого» `sql.py` (можно оставить тонкие обёртки)
- [x] Параметризация `%s` / `cursor.execute(sql, params)` для запросов, которые трогает этап
- [x] `with connection` или пул (`psycopg2.pool` / SQLAlchemy); соединения закрываются
- [x] Единый формат результата (список dict / модели), не `result[1]` / `result[2]`
- [x] Убрать `os.system("docker inspect …")` как способ получить host
- [x] Не переписывать «все запросы мира» вне нужных сценариям функций — но новые и затронутые только с параметрами

### Тестировщик

- [x] Репозиторий: поиск клиента по телефону (есть / нет)
- [x] Имя с апострофом не ломает INSERT (регрессия SQL-инъекции / кавычек)
- [x] После запроса соединение не утекает (пул: checkout/return; или соединение закрыто)
- [x] Негатив: невалидные параметры не интерполируются в текст SQL (моки курсора / echo)

### Код-ревью

- [x] Саммари заполнено
- [x] Вердикт: `MERGE` / `NO MERGE`

**Саммари ревью:**

```
Вердикт: MERGE
Что сделано: PostgresRepository + Protocol KateFitRepository; пул ThreadedConnectionPool с with/putconn. Запросы сценариев бота на %s. sql.py — тонкие обёртки, list[dict], без result[1]/[2] и docker inspect. Пустой SQL_HOST — ConfigError. insert_in_schedule — транзакция, не BEGIN/COMMIT строкой. tests/test_repository.py: поиск, апостроф, пул, мок SQL. pytest tests/ — 67 passed.
Риски: пул создаётся при импорте sql.py (нужна живая БД). used_count смотрит на _pool._used. Нет интеграционных тестов insert_in_schedule / accounting (не требовались чекбоксами). import bot.py/sql.py по-прежнему нужен полный конфиг (этап 10).
Блокеры: нет
```

### Результат проверки

- [x] Автотесты написаны
- [x] Автотесты зелёные
- [x] Код-ревью проведено
- [x] Можно мержить

**Прогон:** `pytest tests/` — 67 passed (с `DATABASE_URL` на `*:5433/Kate_fitness_test`)  
**Вердикт:** MERGE  
**Саммари:** Репозиторий параметризован, соединения возвращаются в пул, бот читает `list[dict]`. Можно переходить к этапу 4.

---

## Этап 4. Сервис клиентов

**Зачем:** создание клиента без Telegram; один код для бота и будущего API.

**Критерий готовности:** `ClientService` создаёт/ищет клиента; бот вызывает сервис, а не сырой SQL для этого сценария.

### Разработчик

- [ ] `ClientService`: создать клиента, найти по телефону, список (недавно ходившие / все)
- [ ] Повтор того же телефона — явная ошибка уникальности, не путается с невалидным номером
- [ ] Хендлер «➕ Новый клиент» вызывает сервис
- [ ] Сообщение об ошибке формата vs «уже существует» остаётся раздельным

### Тестировщик

- [ ] Юнит/интеграция: создать клиента с нормализованным телефоном
- [ ] Повтор телефона — ошибка уникальности
- [ ] Невалидный номер — ошибка валидации, запись в БД не появляется
- [ ] Список клиентов: фабрика нескольких записей, порядок/поля стабильны (не индекс callback)

### Код-ревью

- [ ] Саммари заполнено
- [ ] Вердикт: `MERGE` / `NO MERGE`

**Саммари ревью:**

```
Вердикт:
Что сделано:
Риски:
Блокеры:
```

### Результат проверки

- [ ] Автотесты написаны
- [ ] Автотесты зелёные
- [ ] Код-ревью проведено
- [ ] Можно мержить

**Прогон:**  
**Вердикт:**  
**Саммари:**

---

## Этап 5. Персональная запись

**Зачем:** главное деньги-сценарий бота тестируется без polling.

**Критерий готовности:** `BookingService.book_personal(...)` занимает слот; повтор той же даты/времени — отказ.

### Разработчик

- [ ] `BookingService.book_personal`: клиент, тип (`group_train = false`), дата, время, цена, предоплата
- [ ] Свободные слоты считаются в сервисе/чистой функции, не только в хендлере
- [ ] `bot.py` для персональной записи вызывает сервис (insert не дублирует правила)
- [ ] Баг: `confirm_add` не падает, если `client_multi` is `None` (персональная ветка)
- [ ] Баг: после выбора типа не оставлять хрупкий `process = None` без явной замены шага (минимум — не ломать текущий сценарий; полная унификация — этап 9)

### Тестировщик

- [ ] Персональная запись занимает слот
- [ ] Повтор на ту же дату/время — отказ
- [ ] Цена из предоплаты подставляется, если пакет один
- [ ] Свободные слоты: занятый час отсутствует в выдаче
- [ ] Персональная запись не требует `client_multi`

### Код-ревью

- [ ] Саммари заполнено
- [ ] Вердикт: `MERGE` / `NO MERGE`

**Саммари ревью:**

```
Вердикт:
Что сделано:
Риски:
Блокеры:
```

### Результат проверки

- [ ] Автотесты написаны
- [ ] Автотесты зелёные
- [ ] Код-ревью проведено
- [ ] Можно мержить

**Прогон:**  
**Вердикт:**  
**Саммари:**

---

## Этап 6. Групповая запись

**Зачем:** состав группы читается обратно; сайт потом не парсит строку вслепую.

**Критерий готовности:** групповая запись через сервис; участники сохраняются и читаются; `client = -1` не менять без этапа 11.

### Разработчик

- [ ] `BookingService.book_group`: тип (`group_train = true`), список клиентов, дата, время, цена
- [ ] Запись `client_list` в текущем формате `{id1,id2,...}`; чтение обратно в список id
- [ ] Хендлер группового сценария вызывает сервис
- [ ] Защита: пустой список участников — отказ

### Тестировщик

- [ ] Групповая запись: состав участников читается обратно
- [ ] Пустой список — отказ, слот не занят
- [ ] Слот после группы недоступен для персональной на ту же дату/время
- [ ] Только групповые типы тренировок допускаются в этом use-case

### Код-ревью

- [ ] Саммари заполнено
- [ ] Вердикт: `MERGE` / `NO MERGE`

**Саммари ревью:**

```
Вердикт:
Что сделано:
Риски:
Блокеры:
```

### Результат проверки

- [ ] Автотесты написаны
- [ ] Автотесты зелёные
- [ ] Код-ревью проведено
- [ ] Можно мержить

**Прогон:**  
**Вердикт:**  
**Саммари:**

---

## Этап 7. Предоплата

**Зачем:** списание пакета — ядро учёта; сейчас связь с занятиями эвристическая.

**Критерий готовности:** `PrepaidService` создаёт пакет и списывает занятия; два открытых пакета — явное правило (как в боте: блок).

### Разработчик

- [ ] `PrepaidService.add_package(client, type_train, summ, count)`
- [ ] Списание при записи занятия; на последней — `is_complete = true`
- [ ] Два незакрытых аванса: отказ (текущее поведение бота), состояние не остаётся «грязным»
- [ ] Не делить на ноль; не джойнить «чужие» занятия без привязки к пакету — минимум документировать текущую эвристику и покрыть тестом; явный `accounting_id` — этап 11
- [ ] Хендлер «Добавить оплату» вызывает сервис

### Тестировщик

- [ ] Пакет: `"1000 10"` → 10 занятий, цена за тренировку
- [ ] N занятий списываются из пакета; N+1-е уже не из пакета
- [ ] Последняя тренировка пакета выставляет `is_complete`
- [ ] Два открытых пакета — отказ
- [ ] Ноль тренировок во вводе — валидационная ошибка, строки accounting нет

### Код-ревью

- [ ] Саммари заполнено
- [ ] Вердикт: `MERGE` / `NO MERGE`

**Саммари ревью:**

```
Вердикт:
Что сделано:
Риски:
Блокеры:
```

### Результат проверки

- [ ] Автотесты написаны
- [ ] Автотесты зелёные
- [ ] Код-ревью проведено
- [ ] Можно мержить

**Прогон:**  
**Вердикт:**  
**Саммари:**

---

## Этап 8. Отчёт по прибыли

**Зачем:** формула `income = sum(price) - sum(rent_debt)` должна быть в одном месте.

**Критерий готовности:** `ReportService.monthly_income` совпадает с формулой; хендлер только рисует текст.

### Разработчик

- [ ] `ReportService`: помесячный отчёт из `schedule`
- [ ] Хендлер «💰 Отчет по тренировкам» вызывает сервис
- [ ] Пустой месяц — нули, не падение

### Тестировщик

- [ ] Месяц с персональными и групповыми: `sum(price) - sum(rent_debt)`
- [ ] Пустой месяц — 0
- [ ] `rent_debt` уменьшает итог
- [ ] Границы месяца: занятие 1-го и последнего дня входит, соседний месяц — нет

### Код-ревью

- [ ] Саммари заполнено
- [ ] Вердикт: `MERGE` / `NO MERGE`

**Саммари ревью:**

```
Вердикт:
Что сделано:
Риски:
Блокеры:
```

### Результат проверки

- [ ] Автотесты написаны
- [ ] Автотесты зелёные
- [ ] Код-ревью проведено
- [ ] Можно мержить

**Прогон:**  
**Вердикт:**  
**Саммари:**

---

## Этап 9. Состояние сценария по `chat_id`

**Зачем:** тесты не влияют друг на друга; готовность ко второму устройству и сайту.

**Критерий готовности:** один объект состояния вместо `current_data` + `CurrentData`; ключ — `chat_id` / `user_id`.

### Разработчик

- [ ] Один класс/хранилище сценария (поля из `current_data_clear` + оплата)
- [ ] Состояние по `chat_id` (даже если пользователь пока один)
- [ ] Сброс при `/start` и входе в сценарий — только для этого чата
- [ ] Не вводить полноценный multi-user продукт сверх ключа состояния

### Тестировщик

- [ ] Два `chat_id` независимы: шаг одного не меняет другого
- [ ] `clear` одного чата не сбрасывает второй
- [ ] Сценарий оплаты и записи не делят глобальный синглтон
- [ ] Повторный `/start` в чате A не ломает незавершённый сценарий чата B

### Код-ревью

- [ ] Саммари заполнено
- [ ] Вердикт: `MERGE` / `NO MERGE`

**Саммари ревью:**

```
Вердикт:
Что сделано:
Риски:
Блокеры:
```

### Результат проверки

- [ ] Автотесты написаны
- [ ] Автотесты зелёные
- [ ] Код-ревью проведено
- [ ] Можно мержить

**Прогон:**  
**Вердикт:**  
**Саммари:**

---

## Этап 10. Бот как тонкий адаптер

**Зачем:** хендлеры тестируются без `infinity_polling` и живого токена.

**Критерий готовности:** `import bot` не стартует polling; хендлеры дергают сервисы; callback не завязан только на индекс списка там, где трогаем код.

### Разработчик

- [ ] Не вызывать `bot.infinity_polling()` при `import bot` (`if __name__ == "__main__"`)
- [ ] Клавиатуры и тексты — функции, возвращающие markup/текст
- [ ] Сервисы как зависимость (или модуль), хендлеры без прямого SQL
- [ ] По возможности: `callback_data` = id сущности, не индекс в `list_*` (проверить все ветки `callback_inline` для затронутых кнопок)
- [ ] Разобрать дубли `@bot.message_handler(commands=['show_time'])`: пустой `confirm_add` vs `show_available_time`
- [ ] `main.py` / пустой `pipelines.py` не использовать для фич; удалить или не трогать мёртвый код — по согласованию, без расширения

### Тестировщик

- [ ] Импорт модуля бота не требует валидного токена / не стартует polling (мок конфига)
- [ ] По тексту кнопки / callback вызывается нужный use-case (мок `TeleBot.send_message`)
- [ ] Бизнес-проверки **не** дублировать: достаточно что сервис вызван с ожидаемыми аргументами
- [ ] Регрессия: «В главное меню» сбрасывает состояние текущего `chat_id`

### Код-ревью

- [ ] Саммари заполнено
- [ ] Вердикт: `MERGE` / `NO MERGE`

**Саммари ревью:**

```
Вердикт:
Что сделано:
Риски:
Блокеры:
```

### Результат проверки

- [ ] Автотесты написаны
- [ ] Автотесты зелёные
- [ ] Код-ревью проведено
- [ ] Можно мержить

**Прогон:**  
**Вердикт:**  
**Саммари:**

---

## Этап 11. Модель данных под сайт

**Зачем:** нормальные URL, FK, отчёты «занятия клиента», корректное списание пакета.

**Критерий готовности:** миграция данных применена; бот работает на новой модели; интеграционные тесты обновлены.

**Осторожно:** ломающее изменение. Отдельный срез, не смешивать с API.

### Разработчик

- [ ] Surrogate PK клиента (`id`); телефон — уникальное поле; миграция ссылок `schedule.client`, `accounting.client_id`
- [ ] Участники: `INTEGER[]` или `schedule_participant` вместо строки `{1,2,3}` и `client = -1`
- [ ] `accounting_id` на `schedule` — явная связь занятия с пакетом
- [ ] UNIQUE `(date, time)` или явное правило пересечений слотов
- [ ] CHECK цены ≥ 0, FK, NOT NULL на ключевых полях — после прогона на копии данных
- [ ] Статус занятия (запланировано / проведено / отмена) — только если правило `spend` уточнено; иначе не выдумывать

### Тестировщик

- [ ] Миграция: старый клиент-телефон → `id`, занятия и авансы на том же человеке
- [ ] Группа: участники через новую модель читаются списком id
- [ ] Списание только с указанного `accounting_id`, не «чужие» занятия клиента
- [ ] Конфликт слота: вторая запись на ту же дату/время — отказ (409-логика на уровне сервиса)
- [ ] Смена телефона клиента не отвязывает историю занятий

### Код-ревью

- [ ] Саммари заполнено
- [ ] Вердикт: `MERGE` / `NO MERGE`

**Саммари ревью:**

```
Вердикт:
Что сделано:
Риски:
Блокеры:
```

### Результат проверки

- [ ] Автотесты написаны
- [ ] Автотесты зелёные
- [ ] Код-ревью проведено
- [ ] Можно мержить

**Прогон:**  
**Вердикт:**  
**Саммари:**

---

## Этап 12. HTTP API и авторизация

**Зачем:** сайт не ходит в БД и не копирует правила предоплаты.

**Критерий готовности:** FastAPI поверх тех же сервисов; OpenAPI; контрактные тесты; бот с whitelist `chat_id`.

### Разработчик

- [ ] FastAPI: клиенты, типы тренировок, слоты, записи, предоплаты, отчёт
- [ ] Pydantic-модели запросов/ответов; OpenAPI
- [ ] Авторизация API (сессия/JWT)
- [ ] Whitelist `chat_id` для бота
- [ ] Идемпотентность / 409 на занятый слот
- [ ] Пагинация и поиск клиентов
- [ ] Сайт **не** начинать в этом этапе (только API + тесты)

### Тестировщик

- [ ] Контракт: схема ответа клиентов и записей (OpenAPI / snapshot)
- [ ] 401 без токена
- [ ] 409 на занятый слот
- [ ] Создание клиента и предоплаты через API совпадает с сервисами (те же инварианты, что этапы 4 и 7)
- [ ] Бот: пользователь не из whitelist не меняет данные (мок)

### Код-ревью

- [ ] Саммари заполнено
- [ ] Вердикт: `MERGE` / `NO MERGE`

**Саммари ревью:**

```
Вердикт:
Что сделано:
Риски:
Блокеры:
```

### Результат проверки

- [ ] Автотесты написаны
- [ ] Автотесты зелёные
- [ ] Код-ревью проведено
- [ ] Можно мержить

**Прогон:**  
**Вердикт:**  
**Саммари:**

---

## Что не делать, пока не закрыты этапы 0–10

- сайт на тех же таблицах напрямую;
- FSM-фреймворк Telegram «для красоты»;
- параметризация всех SQL отдельным PR без репозитория (этап 3);
- смена `client` = телефон и формата `client_list` до этапа 11;
- multi-user продукт в боте до этапа 9;
- E2E через реальный Telegram вместо ядра.

---

## Как вести работу в Cursor

1. Открыть этот файл, выбрать **один** незакрытый этап сверху вниз.
2. Запустить субагента **разработчик** с промптом этапа.
3. Запустить субагента **тестировщик** — результат этапа = автотесты.
4. Запустить субагента **код-ревьювер** — получить саммари и `MERGE` / `NO MERGE`.
5. Заполнить **Результат проверки** (отдельные чекбоксы). Без них этап не закрыт.
6. Обновить строку в таблице «Статус этапов».
7. Коммит — только по просьбе пользователя, сообщение на русском.

Отчёты по закрытым этапам можно складывать в `refactoring/reports/stage-N.md` по шаблону выше.
